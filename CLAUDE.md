# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Information

**Repository:** XianingY/vstalk
**Status:** New/Empty repository - No code committed yet

## Development Workflow

This repository is currently empty. As the project develops:

### Common Commands to Add

Once the codebase is established, this section should include:

- **Build commands:** Instructions for compiling/building the project
- **Test commands:** How to run tests, including single test execution
- **Linting:** Code style and quality checks
- **Development server:** Local development setup
- **Installation:** How to install dependencies

### Project Architecture

This repository is in its initial state. When code is added, describe:

- **High-level architecture:** Overall design patterns and structure
- **Key directories:** Important folders and their purposes
- **Core modules:** Main components and their relationships
- **Technology stack:** Programming languages, frameworks, and tools used

## Adding Content to This File

As you develop this project, populate this file with:

1. **Commands Section:**
   ```markdown
   ## Development Commands

   ### Building
   ```bash
   # Command to build the project
   npm run build
   ```

   ### Testing
   ```bash
   # Run all tests
   npm test

   # Run a single test file
   npm test -- path/to/test.file.test.js
   ```

   ### Linting
   ```bash
   # Check code style
   npm run lint

   # Fix linting issues
   npm run lint:fix
   ```
   ```

2. **Architecture Section:**
   ```markdown
   ## Code Architecture

   This project follows a [architecture pattern, e.g., MVC, microservices, monorepo].

   ### Directory Structure
   ```
   /src
     /components    # React components
     /services      # API and business logic
     /utils         # Helper functions
   /tests          # Test files
   ```

   ### Key Components
   - **Component Name:** Brief description and purpose
   - **Another Component:** Brief description and purpose
   ```
   ```

3. **Configuration Files**
   - Document important config files (e.g., `.env`, `config.json`)
   - Explain required environment variables
   - Note any special setup requirements

## Cursor/Copilot Rules

If this project has specific development guidelines in `.cursor/rules/` or `.github/copilot-instructions.md`, they will be documented here.

## Best Practices for This Repository

- [To be filled as the project develops]
